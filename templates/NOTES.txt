Kurrier has been installed.

Web service:
  {{ include "kurrier.web.serviceName" . }}:{{ .Values.web.service.port }}

Worker service:
  {{ include "kurrier.worker.serviceName" . }}:{{ .Values.worker.service.port }}

{{- if .Values.ingress.enabled }}
Ingress hosts:
{{- range .Values.ingress.hosts }}
  - https://{{ .host }}
{{- end }}
{{- else if .Values.gateway.enabled }}
Gateway hostnames:
{{- range .Values.gateway.hostnames }}
  - https://{{ . }}
{{- end }}
{{- else }}
Ingress/Gateway is disabled. Port-forward for local testing:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kurrier.web.serviceName" . }} 3000:{{ .Values.web.service.port }}
{{- end }}

The migration Job is a Helm hook:
  {{ include "kurrier.fullname" . }}-migrate
